import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cut_the_bill',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
